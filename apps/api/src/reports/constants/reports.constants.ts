/** Radius used when linking a new report to an existing duplicate group (same rough location). */
export const DUPLICATE_RADIUS_METERS = 30;

/** Radius when matching a submission to an existing site during geospatial search. */
export const SITE_NEARBY_RADIUS_METERS = 50;
