/** Per-file image cap for citizen report/event/site uploads (matches mobile prep ceiling). */
export const CITIZEN_IMAGE_UPLOAD_MAX_BYTES = 12 * 1024 * 1024;

/** Max images per multipart request on report/resolution upload endpoints. */
export const CITIZEN_IMAGE_UPLOAD_MAX_FILES = 5;
