# Documented exemptions: `AppBootstrap.instance` may only be read from the app
# composition root (lib/main.dart), which wires the root ProviderScope before
# any widget/provider exists. All other code must use Riverpod providers /
# `readRoot`. The ratchet forbids new entries.
lib/main.dart:159
