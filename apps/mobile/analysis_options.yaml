# The app shell (lib/) uses the same curated workspace baseline as every
# package so one lint set governs the whole repo. See packages/analysis_options.yaml
# for the rule rationale. CI runs this with `--fatal-infos`.
include: packages/analysis_options.yaml
