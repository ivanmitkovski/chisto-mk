/// One line from API `pointsBreakdown` on report submit.
class ReportSubmitPointsBreakdownLine {
  const ReportSubmitPointsBreakdownLine({
    required this.code,
    required this.points,
  });

  final String code;
  final int points;
}
