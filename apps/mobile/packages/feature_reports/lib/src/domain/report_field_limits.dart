/// Canonical field limits for the pollution report wizard (domain layer).
class ReportFieldLimits {
  const ReportFieldLimits._();

  static const int maxTitleLength = 120;
  static const int maxDescriptionLength = 500;
  static const int maxPhotos = 5;
}
