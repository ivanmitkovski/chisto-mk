import 'package:flutter_map/flutter_map.dart';

/// Stub: returns null on web (dart:io unavailable). Use default TileProvider.
TileProvider? createCachedTileProvider({int maxStaleDays = 30}) => null;
