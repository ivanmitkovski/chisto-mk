name: chisto_persistence
description: Secure storage, SQLite helpers, and image disk caches for Chisto mobile.
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  cached_network_image: ^3.4.1
  flutter:
    sdk: flutter
  flutter_cache_manager: ^3.4.0
  flutter_secure_storage: ^10.0.0
  path_provider: ^2.1.5
  sqflite: ^2.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
